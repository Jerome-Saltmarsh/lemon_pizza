
import 'package:flutter/material.dart';

class ColorSchemes {
  static final light = ColorScheme.fromSeed(seedColor: Colors.green);
  static final dark = ColorScheme.fromSeed(seedColor: Colors.blue);
}