
import 'package:flutter/material.dart';

class ScaffoldMobile extends StatelessWidget {
  const ScaffoldMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
         title: const Text("PIZZA MOBILE"),
       ),
    );
  }

}